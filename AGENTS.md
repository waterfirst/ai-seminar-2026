# Token-Frugal Work Rules

- 먼저 저장소에서 기존 코드, 문서, Skill을 검색한다.
- 전체 파일 대신 필요한 범위만 읽는다.
- 목표, 입력, 산출물, 제약, 완료 조건을 확인한 뒤 작업한다.
- 반복 가능한 결정론적 작업은 Python·SQL·쉘로 처리한다.
- 원천 데이터는 변경하지 않고 분석용 변환과 결과를 분리한다.
- 테이블·컬럼의 의미, 단위, 오너, 보안등급을 확인한다.
- 분석 데이터는 가능하면 tidy long format을 사용한다.
- PPT·Word 전에 Markdown·HTML 원본을 남긴다.
- 같은 실패가 두 번 반복되면 중단하고 원인과 다음 선택지를 보고한다.
- 검증된 결과는 README와 재사용 방법을 남겨 조직 자산으로 만든다.

